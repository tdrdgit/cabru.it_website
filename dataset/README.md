# Dataset richieste di quotazione

`quote-requests-template.xlsx` e' SOLO il template (struttura colonne).

I dati reali NON vanno qui (repo pubblico = problema privacy/GDPR).
Il registro vivo e' il Google Sheet privato "CABRU - Richieste di quotazione" nel Drive di Carlo,
alimentato dal form via Google Apps Script. Da li' si scarica l'.xlsx quando serve.
